# Icons

Place PNG icons here:

- `icon16.png` (16x16)
- `icon48.png` (48x48)
- `icon128.png` (128x128)

These are referenced by `manifest.json`. If missing, Chrome will load the
extension with a default placeholder icon — you can remove the `icons` block
from the manifest if you don't want to add custom icons.
